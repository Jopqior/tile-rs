[package]
name = "tile_hal"
version = "0.0.1"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Hardware Abstraction Layer for multi-backend accelerator dispatch"

[features]
default = []
ascend = ["ascend_rs"]
cuda = ["libloading"]
aie = ["libloading"]

[dependencies]
ascend_rs = { path = "../ascend_rs", optional = true }
libloading = { version = "0.8", optional = true }

[dev-dependencies]
