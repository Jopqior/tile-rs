# tile-rs

tile-rs 将计算描述转换为面向加速器的程序。以下词汇用于区分生成代码的组件与检验计算结果的用例。

## Language

**kernel**：本项目中待生成和执行的计算单元。统一使用 kernel，必要时注明阶段，例如 kernel 的 MLIR 输入、生成的 MSL、编译后的 GPU 程序；具体计算含义由验证用例说明。
_Avoid_：算子

**Metal emitter**：将 MLIR 中的计算描述转换为 MSL 源码的组件。它不是完整的 Rust 编译驱动，也不是执行计算的 Metal 运行环境。
_Avoid_：用“Metal 后端”泛指 emitter、完整编译驱动和运行环境

**正确性验证用例**：具有明确计算含义、输入及预期输出依据的一次验证定义。用于 emitter 验证时，其 MLIR 输入必须与声明的计算含义一致；一个用例通过不代表该 kernel 的其他类型、规模或实现均已通过。
_Avoid_：把一次运行结果或既有输出直接称为正确性标准

**验证产物**：验证过程中保存、供追溯、重新运行用例或复查结果使用的材料统称，包括计算输入、生成程序、运行环境记录或结果比较记录等。重新运行用例是使用保存的输入、源码版本和配置再次执行验证；复查结果是使用保存的输出、参考结果和比较规则检查当时的判定，无需再次运行 GPU。它不只指编译出的 GPU 程序。
_Avoid_：用“编译产物”代指全部验证产物
