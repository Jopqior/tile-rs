# tile-rs

用于编写加速器 kernel 并面向不同硬件目标生成代码的领域词汇。

## Language

**Tile（数据块）**：
kernel 中带二维形状与元素类型的计算数据块。区别于按容量与有效长度描述的缓冲区，不预设其物理存储位置。
_Avoid_：把所有缓冲区统称为 Tile；把 Tile 等同于某种硬件存储。

**硬件目标**：
kernel 预期运行的硬件对象，例如 Apple GPU。区别于面向该硬件使用的目标平台。

**目标平台**：
面向硬件进行编程、编译和执行的软件平台或接口体系，例如 Metal。

**rustc 编译器后端**：
接入 rustc、承担 kernel 后续代码生成的编译组件。区别于单个目标代码生成器和主机运行时。

**目标代码生成器（emitter）**：
把 kernel 的中间表示转换为某种目标代码的组件。生成代码不等于已完成目标编译或硬件执行。

**目标编译工具链**：
把目标代码编译为可供目标执行环境加载的产物所需的工具集合。

**主机运行时**：
在主机侧管理设备资源、加载 kernel、绑定参数并提交执行的软件部分。区别于 kernel 内部的计算。

**目标转换与执行链路**：
kernel 经代码转换、目标编译直至提交执行的阶段关系；明确目标时简称“Metal 链路”等。这个称呼不保证所有阶段均已有实现或经过验证。
_Avoid_：不带限定的“后端”；用“目标后端”笼统代称生成器、工具链、运行时或整条链路。引用源码中的 Backend 等标识符时保留原名并说明职责。

**Device buffer**：
host 管理的设备内存资源。区别于 kernel 中的访问视图和计算数据块，不能因由 host 管理就称为 Host buffer。

**GM view**：
kernel 对 global memory 的带类型和形状的访问视图。它描述访问的区域，不等于资源所有权或 load 后的 Tile。

Device buffer、GM view 与 Tile 不预设一一对应的物理分配关系；逻辑 load/store 的物理实现由具体目标决定。

**Kernel 入口**：
可供主机提交执行的计算入口。一个入口可以包含多个 tile 操作，不与每个 DSL 函数一一对应。

**Tile operation（tile 操作）**：
kernel 内表达计算或数据搬运的操作。操作及其组合可以构成一个 kernel 的实现，不要求每个操作独立提交执行。

### Kernel 的表示与转换

**Rust DSL**：
用 Rust 类型与操作表达 kernel 的语言接口。Tile 类型与 tile 操作集合属于此接口，不能据此推定存在独立的 Tile IR 方言。

**MLIR 文本**：
本项目目标代码生成器所消费或生成的 MLIR 程序文本。应明确所涉及的方言，不能与 Rust DSL 或文本解析结果混称。

**MLIR 文本解析结果**：
从 MLIR 文本提取出的函数、参数、全局声明和函数体文本等结构。这个称呼不承诺完整的 MLIR 语义模型。
_Avoid_：模块摘要；用 Tile IR 指代此结果。

**parse**：
从程序文本中提取结构的动作。在本项目的转换描述中，parse 本身不表示目标实现已经确定。

**lowering**：
把较抽象的 kernel 操作或程序表示落实为更具体实现表示的动作。不要求其结果单独形成一种 IR。

**emit**：
生成目标代码的动作，可与实现选择及 lowering 交织。parse、lowering、emit 是动作区分，不保证对应独立阶段或模块。
